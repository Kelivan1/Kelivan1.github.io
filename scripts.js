function jumpToAboutMe() {
    // Get the header element
    var header = document.getElementById('About-Me-Header');
    
    // Use scrollIntoView to scroll to the header
    header.scrollIntoView({ behavior: 'smooth' });
}

function jumpToProjects() {
    // Get the header element
    var header = document.getElementById('Project-Header');
    
    // Use scrollIntoView to scroll to the header
    header.scrollIntoView({ behavior: 'smooth' });
}